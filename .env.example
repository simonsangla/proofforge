# Vercel Blob token for persistent artifact-draft storage.
# Paste this in Vercel → Project → Settings → Environment Variables
# (scope it to Preview + Production). When unset, drafts fall back to the
# local filesystem at content/artifact-drafts/ (useful for local dev only).
BLOB_READ_WRITE_TOKEN=
